logging:
  level:
    .: error
    org.springframework: ERROR
    com.mkyong: ERROR

spring:
  profiles:
    active: "dev"
  main:
    banner-mode: "off"

---

spring:
  profiles: dev
  application:
    name: demoservice
  datasource:
    username: root
    password: root
    url: jdbc:mysql://192.168.2.1:3306/ems?createDatabaseIfNotExist=true
server:
  port: 8081

---

spring:
  profiles: prod
  application:
    name: ems-backend
  datasource:
    username: root
    password: root
    url:
server:
  port: 3301